package com.mentis.usuario.repository;

import com.mentis.usuario.entity.Area;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AreaRepository extends JpaRepository<Area, Long> {
}
